Favicon cache debug findings:

The local G-shaped favicon derivatives and live served bytes match. The previous browser icon was caused by the browser retaining the old favicon URL. The shared document head now uses cache-busted references:
- /favicon.ico?v=gerome-g
- /favicon-32.png?v=gerome-g
- /favicon-16.png?v=gerome-g
- /apple-touch-icon.png?v=gerome-g

The refreshed live DOM exposes all four cache-busted URLs. Horizontal containment remains clean: document scrollWidth 1265px, viewportWidth 1280px, horizontalOverflow false.

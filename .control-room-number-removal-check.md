## Automation Control Room number-removal verification

The production build passed after removing the visible index spans from the node cards and removing the unused `index` prop from `NodeCard`.

The live DOM verification found zero `.v-control-room-reference-node-index` elements and 13 remaining node cards. The first card now contains only its glyph, label, and detail text. Its computed grid is `18px 75.3906px`, and the detail text correctly occupies grid column 2, reclaiming the former number column.

The visual preview shows the control-room card structure, animated paths, AI core, and output status indicators still intact.

# Editorial graphite canvas preview findings

The live computed-style audit confirms that the hero and Projects pseudo-elements contain static radial-gradient dot textures with `pointer-events: none`, while the Contact terminal has no new texture pseudo-element. The automation figure has the sage radial falloff and no animation assigned to the pseudo-element. No background animation was introduced.

The initial body scroll-width discrepancy came from the intentionally oversized `inset: -20%` on the automation figure’s decorative radial pseudo-element. Adding `overflow: clip` to the figure contained that decorative layer without changing the diagram geometry. The follow-up audit reports matching body and client widths, so the page has no horizontal overflow at the checked viewport.

The refreshed live preview keeps the hero and Projects archive visually intact. The background remains a flat editorial graphite field with restrained localized texture, and dense lower-page content—including the Contact terminal—does not receive the new texture treatment.

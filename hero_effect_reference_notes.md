Reference crop observation: 61x301 portrait image shows a faint vertical hairline/rail near the left edge with four small glowing white dots spaced down the rail. The effect is not visibly present in the current full desktop hero screenshot; the current inline SVG panel shows only its card/status dots, connector paths, and core glow. Source inspection found stale retired control-room CSS in __root.tsx, but no matching rendered v-control-room-reference markup in the current HeroAutomationAnimation component. The current panel has v-panel-card-status dots on input/output cards and connector/core glow styles. Further DOM inspection is needed before removing any effect to avoid deleting the wrong visual.

# Signal Map live-preview findings

The refreshed preview at https://4183-in6c5oavpe8uz6rudhxc5-436cab69.us4.manus.computer/#top shows the new static Signal Map treatment.

Hero inspection confirms the routed hairlines are visible as a restrained architectural layer behind the copy and automation diagram. The trace language echoes the page's Input → AI Layer → Workflow → Output structure, with small sage nodes and terminal-style corner markers. The current diagram, text, buttons, and navigation remain in their existing positions.

The Projects/What I Build inspection confirms the page remains visually legible and the new background is not applied to the What I Build cards. The lower archive will be checked separately for trace containment and text contrast. No external research was used; this is a local preview audit.

Projects anchor inspection shows the routed traces are visible behind the archive header and surrounding whitespace, while the project panel, selected project title, metadata, preview, and View Project Details control remain legible and visually dominant. The trace lines stay outside dense project content rather than crossing card text.

Computed-style audit confirms all new layers are static (`animation: none`), pointer-transparent, and scoped to `.v-hero` / `.ab-atlas-section`. The Contact target has no new pseudo-element layer. At the checked desktop viewport, body/document scroll width is 1265px versus a 1280px viewport, so there is no horizontal overflow.
